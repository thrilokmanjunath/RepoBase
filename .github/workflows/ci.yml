name: CI/CD Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python 3.13
      uses: actions/setup-python@v4
      with:
        python-version: '3.13'
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest pytest-django flake8
    - name: Lint with flake8
      run: |
        flake8 repobase_project --count --select=E9,F63,F7,F82 --show-source --statistics
        flake8 repobase_project --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    - name: Run Tests
      env:
        SECRET_KEY: 'test-secret-key-123'
        DEBUG: 'True'
        ALLOWED_HOSTS: '*'
      run: |
        cd repobase_project
        pytest
